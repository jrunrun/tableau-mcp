# merge-branch-into-current

This command merges a completed branch INTO the current branch.

Current branch = destination (kept)
Provided branch = source (merged)

We combine both changes. Never overwrite one branch entirely.

This command will be available in chat with /merge-branch-into-current
