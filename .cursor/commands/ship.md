# ship

Analyze the current git diff. Generate a clear, concise commit message using conventional commits format. Then stage all changes, commit, and push to the current branch.

This command will be available in chat with /ship
